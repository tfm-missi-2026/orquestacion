meta {
  name: Acceso sin token
  type: http
  seq: 3
}

get {
  url: {{tfm-api-gateway-local}}/api/usuarios
  body: none
  auth: none
}

tests {
  test("endpoint protegido sin token 401", function() {
    expect(res.status).to.equal(401);
  });
}
