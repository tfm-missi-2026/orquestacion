meta {
  name: Gateway health
  type: http
  seq: 1
}

get {
  url: {{tfm-api-gateway-local}}/actuator/health
  body: none
  auth: none
}

tests {
  test("responde 200", function() {
    expect(res.status).to.equal(200);
  });
  test("estado UP", function() {
    expect(res.body.status).to.equal("UP");
  });
}
