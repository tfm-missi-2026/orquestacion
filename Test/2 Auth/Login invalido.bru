meta {
  name: Login invalido
  type: http
  seq: 2
}

post {
  url: {{tfm-api-gateway-local}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "admin@srp.local",
    "contrasenia": "claveIncorrecta"
  }
}

tests {
  test("credenciales invalidas 401", function() {
    expect(res.status).to.equal(401);
  });
}
