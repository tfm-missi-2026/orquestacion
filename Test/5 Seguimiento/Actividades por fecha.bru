meta {
  name: Actividades por fecha
  type: http
  seq: 9
}

get {
  url: {{tfm-api-gateway-local}}/api/actividades/por-fecha?fecha=2026-06-02
  body: none
  auth: bearer
}

params:query {
  fecha: 2026-06-02
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responde 200", function() {
    expect(res.status).to.equal(200);
  });
  test("devuelve lista", function() {
    expect(res.body).to.be.an("array");
  });
}
