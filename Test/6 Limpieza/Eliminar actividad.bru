meta {
  name: Eliminar actividad
  type: http
  seq: 4
}

delete {
  url: {{tfm-api-gateway-local}}/api/actividades/{{actividadId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "motivoEliminacion": "Limpieza de pruebas QA"
  }
}

tests {
  test("eliminada (200/204)", function() {
    expect(res.status).to.be.oneOf([200, 204]);
  });
}
