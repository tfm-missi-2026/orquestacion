meta {
  name: Acceso sin token
  type: http
  seq: 2
}
get {
  url: {{baseUrl}}/api/usuarios
  body: none
  auth: none
}
tests {
  test("el endpoint protegido responde 401", function() {
    expect(res.status).to.equal(401);
  });
}
