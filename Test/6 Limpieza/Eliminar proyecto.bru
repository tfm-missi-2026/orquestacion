meta {
  name: Eliminar proyecto
  type: http
  seq: 7
}

delete {
  url: {{tfm-api-gateway-local}}/api/proyectos/{{proyectoId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "motivoEliminacion": "Limpieza de pruebas QA"
  }
}

tests {
  test("eliminado (200/204)", function() {
    expect(res.status).to.be.oneOf([200, 204]);
  });
}
