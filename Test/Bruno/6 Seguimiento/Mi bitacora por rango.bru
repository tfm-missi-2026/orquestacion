meta {
  name: Mi bitacora por rango
  type: http
  seq: 13
}
get {
  url: {{baseUrl}}/api/bitacora/mia/rango?desde=2026-08-03&hasta=2026-08-07
  body: none
  auth: bearer
}
auth:bearer {
  token: {{token}}
}
tests {
  test("responde 200", function() { expect(res.status).to.equal(200); });
  test("devuelve los registros del rango", function() {
    expect(res.body.items || res.body).to.be.an("array");
  });
}
