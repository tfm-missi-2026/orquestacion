meta {
  name: Reset password
  type: http
  seq: 20
}
post {
  url: {{baseUrl}}/api/usuarios/{{usuarioCreadoId}}/reset-password
  body: json
  auth: bearer
}
auth:bearer {
  token: {{token}}
}
body:json {
  {
    "contrasenia": "Qa.2026Nueva"
  }
}
tests {
  test("responde 200 o 204", function() { expect(res.status).to.be.oneOf([200, 204]); });
}
