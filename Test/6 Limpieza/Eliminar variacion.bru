meta {
  name: Eliminar variacion
  type: http
  seq: 1
}

delete {
  url: {{tfm-api-gateway-local}}/api/variaciones/{{variacionId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "motivoEliminacion": "Limpieza de pruebas QA"
  }
}

tests {
  test("eliminada (200/204)", function() {
    expect(res.status).to.be.oneOf([200, 204]);
  });
}
