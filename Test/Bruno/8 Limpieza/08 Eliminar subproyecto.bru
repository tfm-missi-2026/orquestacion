meta {
  name: Eliminar subproyecto
  type: http
  seq: 8
}
delete {
  url: {{baseUrl}}/api/subproyectos/{{subproyectoId}}
  body: json
  auth: bearer
}
auth:bearer {
  token: {{token}}
}
body:json {
  {
    "motivoEliminacion": "Limpieza de la corrida {{runId}}"
  }
}
tests {
  test("eliminado (204)", function() { expect(res.status).to.equal(204); });
}
