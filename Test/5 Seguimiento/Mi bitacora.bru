meta {
  name: Mi bitacora
  type: http
  seq: 13
}

get {
  url: {{tfm-api-gateway-local}}/api/bitacora/mia?fecha=2026-06-02
  body: none
  auth: bearer
}

params:query {
  fecha: 2026-06-02
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responde 200", function() {
    expect(res.status).to.equal(200);
  });
  test("devuelve lista del dia", function() {
    expect(res.body).to.be.an("array");
  });
}
